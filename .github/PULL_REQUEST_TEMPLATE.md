## What and why

## Checks

- [ ] `python -m unittest discover -s tests`
- [ ] `project-observatory full check` (or the suites this touches)
- [ ] `python tools/update_inventory.py --check` after engine changes
- [ ] `python tools/check_public_release.py --history`
- [ ] CHANGELOG entry for user-visible changes

No credential values, private project names, local paths or real dashboard captures in code, tests or this description.
